# ESP32 Buddy — headless ESP-NOW client for the Flipper-Zero-ESP32 port master.
# Standard ESP-IDF project (tested against IDF v5.4.1, same as the master).
cmake_minimum_required(VERSION 3.16)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(esp32_buddy)
